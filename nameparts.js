"use strict";

const nameStr = "Rachel Duc Vo";
const firstName = nameStr.substring(0, 6);
const middleName = nameStr.substring(6, 10);
const lastName = nameStr.substring(10, 13);

console.log(`My name is ${nameStr}
First name is ${firstName}
Middle name is ${middleName}
Last name is ${lastName}`);